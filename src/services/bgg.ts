/**
 * BGG API Service integrating with XML endpoint
 * https://boardgamegeek.com/xmlapi2/collection\?username\=lumiguia\&stats\=1
 */

// Uses fetch API to pull data with retry + exponential backoff later
export async function syncBGGCollection() {
  // Stub for now. Week 2 task as per PRD but scaffolding here.
  return [];
}
